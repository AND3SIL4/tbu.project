import { useEffect, useState } from "react";

const Home = () => {
  const [taskBots, setTaskBots] = useState([]);

  return (
    <div>
      <h2>Task bot list</h2>
    </div>
  );
};

export default Home;
